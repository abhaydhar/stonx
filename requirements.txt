# Core Dependencies
python-dotenv==1.2.2
pydantic==2.12.5
pydantic-settings==2.10.1

# Data & Analysis
pandas==2.2.0
numpy==1.26.0
yfinance==0.2.37
pyarrow==24.0.0
nsepy==0.8

# Technical Analysis
# The current scanner modules use pandas/numpy directly.
# pandas-ta and TA-Lib are intentionally omitted from the default install:
# pandas-ta==0.3.14b is no longer resolvable on PyPI, and TA-Lib requires
# native build dependencies on Windows/Python 3.12. Add them back only when
# code imports them and CI images provide the required native libraries.

# Agent Framework
crewai==1.15.2
langchain==0.1.20
langchain-anthropic==0.1.11
anthropic==0.25.0
openai==2.44.0  # OpenAI-compatible client, used to reach Gemini's OpenAI-compatible endpoint

# Database
sqlalchemy==2.0.28
psycopg2-binary==2.9.9

# Dashboard & Visualization
streamlit==1.32.0
plotly==5.20.0

# Alerting
apprise==1.7.4
python-telegram-bot==22.8

# Scheduling
apscheduler==3.10.4

# Testing
pytest==8.1.1
pytest-mock==3.12.0

# Logging
structlog==24.1.0

# Backtesting
backtesting==0.3.3
vectorbt==0.26.1

# Utilities
requests==2.31.0
beautifulsoup4==4.12.3
